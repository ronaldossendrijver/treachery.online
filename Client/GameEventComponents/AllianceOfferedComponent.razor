<!--
 * Copyright 2020-2021 Ronald Ossendrijver. All rights reserved.
--->

@using Treachery.Shared
@using Treachery.Client.GenericComponents;
@inherits GameEventComponent<AllianceOffered>

<GameEventComponentLayout Border="@Border" BackgroundStyle="@BackgroundStyle">

    <Header>Offer a faction to form an Alliance</Header>

    <Body>
        <SelectFactionComponent @bind-Value="target" Values="Game.ValidTargets(Player)" />
        <ButtonRowComponent Confirm="Confirm" ConfirmText="Confirm" ConfirmError="@validationError" />
    </Body>

</GameEventComponentLayout>

@code {

    private Faction target;

    protected override void OnAfterRender(bool firstRender)
    {
        //Initialize<Faction, Faction>(ref target, Game.ValidTargets(Player));
    }

    protected override AllianceOffered ConfirmedResult => new AllianceOffered(Game) { Initiator = Faction, Target = target };
}

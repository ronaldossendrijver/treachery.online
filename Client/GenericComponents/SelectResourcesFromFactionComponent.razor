<!--
 * Copyright 2020-2021 Ronald Ossendrijver. All rights reserved.
--->

@using Treachery.Shared;

<div class="d-inline-block justify-content-start">

    @if (Label != "")
    {
        <label class="mt-1 align-content-center">@Label</label>
    }

    <NumberPickerComponent Disabled="@Disabled" @bind-Value="Value" Min="Min" Max="Max"
                           SymbolSRC="@Skin.Current.GetImageURL(Faction)" SymbolBackgroundFillColor="white" SymbolBackgroundBorderColor="#FF5400" 
                           NumberAlignment="NumberAlignment.BottomRight" NumberBackgroundFillColor="white" NumberBackgroundBorderColor="#FF5400" BehindNumberSRC="@Skin.Current.Harvester_URL"/>
</div>

@code {

    private int _value;

    [Parameter]
    public int Value
    {
        get
        {
            return _value;
        }
        set
        {
            if (_value != value)
            {
                _value = value;
                ValueChanged.InvokeAsync(value);
                OnChanged?.Invoke();
            }
        }
    }

    [Parameter]
    public EventCallback<int> ValueChanged { get; set; }

    [Parameter]
    public Action OnChanged { get; set; }

    [Parameter]
    public EventCallback OnUp { get; set; }

    [Parameter]
    public EventCallback OnDown { get; set; }

    [Parameter]
    public int Min { get; set; }

    [Parameter]
    public int Max { get; set; }

    [Parameter]
    public string Label { get; set; } = "";

    [Parameter]
    public bool Disabled { get; set; } = false;

    [Parameter]
    public Faction Faction { get; set; } = Faction.None;
}

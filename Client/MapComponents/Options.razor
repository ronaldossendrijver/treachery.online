@using Treachery.Shared
@inherits MapComponent

<Image X="@(Skin.Current.MapDimensions.X - 100)" Y="@(Skin.Current.MapDimensions.Y / 4 - 250)" Diameter="@(100)" Src="@(h.ShowWheelsAndHMS ? Skin.Current.Eye_URL : Skin.Current.EyeSlash_URL)" OnClick="OnClick" Title="Show/Hide Battle Wheel and HMS"/>

@code {

    private void OnClick(MouseEventArgs e)
    {
        h.ShowWheelsAndHMS = !h.ShowWheelsAndHMS;
    }
}
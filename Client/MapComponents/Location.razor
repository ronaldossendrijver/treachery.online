@using Treachery.Shared 
@inherits MapComponent

<Territory ToShow="@ToShow.Territory" FillColor="@FillColor" FillOpacity="@FillOpacity" StrokeColor="transparant" StrokeWidth="0" Sector=@ToShow.Sector/>

@code {

    //A rx ry x-axis-rotation large-arc-flag sweep-flag x y

    [Parameter]
    public Treachery.Shared.Location ToShow { get; set; }

    [Parameter]
    public string FillColor { get; set; }

    [Parameter]
    public float FillOpacity { get; set; } = 0f;

    [Parameter]
    public string FillRule { get; set; } = "";
}

